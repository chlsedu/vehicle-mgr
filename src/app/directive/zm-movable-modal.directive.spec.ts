import { ZmMovableModalDirective } from './zm-movable-modal.directive';

describe('ZmMovableModalDirective', () => {
  it('should create an instance', () => {
    const directive = new ZmMovableModalDirective();
    expect(directive).toBeTruthy();
  });
});
